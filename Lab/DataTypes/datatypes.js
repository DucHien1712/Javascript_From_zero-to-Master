// number
const age = 25;
const price = 19.99;

// string
const name = "John Doe";
const greeting = 'Hello, World!';

// boolean
const isStudent = true;
const hasGraduated = false; 

// undefined: chưa định nghĩa
let address;
console.log(address); // Output: undefined

// null: đã định nghĩa nhưng không có giá trị
let phoneNumber = null;
console.log(phoneNumber); // Output: null

// object: key-value
const person = {
    name: "Alice",
    age: 30,
    address: "HCM"
};

console.log(person)

// array
const numbers = [1, 2, 3, 4, 5];
console.log(numbers);
